# Simple Map Example

This example builds a 10x10 overworld using the declarative map scene API, and then adds a handler when NPCs are interacted upon to spawn UI on top of all scenes

## Running

From the repository root:

```bash
python -m example.spawn_ui.main
```